# ─────────────────────────────────────────────────────────────────────────────
# J.A.R.V.I.S. — Production Requirements
# Python 3.9+ recommended
# Last updated: 2026-08-19
# ─────────────────────────────────────────────────────────────────────────────

# ── Core AI & Language ────────────────────────────────────────────────────────
google-generativeai>=0.8.0       # Gemini 2.0 Flash LLM brain
wikipedia>=1.4.0                 # Wikipedia summaries

# ── Environment & Configuration ───────────────────────────────────────────────
python-dotenv>=1.0.0             # .env file loading

# ── Voice & Audio ─────────────────────────────────────────────────────────────
pyttsx3>=2.90                    # Offline text-to-speech
SpeechRecognition>=3.10.0        # Voice recognition via Google API
PyAudio>=0.2.13                  # Microphone access (see install note below)

# ── System & Hardware ─────────────────────────────────────────────────────────
psutil>=5.9.0                    # CPU, RAM, battery, disk telemetry
PyAutoGUI>=0.9.54                # Screenshot capture, volume control
pywin32>=306                     # Windows OS integration
pypiwin32>=223                   # Windows COM support

# ── Network & Web ─────────────────────────────────────────────────────────────
requests>=2.31.0                 # HTTP client for weather & location APIs
feedparser>=6.0.8                # RSS news feed parser
geocoder>=1.38.1                 # IP-based geolocation fallback
beautifulsoup4>=4.12.0           # HTML parsing
urllib3>=2.0.0                   # HTTP transport layer

# ── Terminal & Display ────────────────────────────────────────────────────────
colorama>=0.4.6                  # Cross-platform terminal color support

# ── Utility ───────────────────────────────────────────────────────────────────
pyjokes>=0.6.0                   # Programmer jokes
Pillow>=10.0.0                   # Image handling (screenshots)

# ─────────────────────────────────────────────────────────────────────────────
# Installation Notes:
#
# PyAudio on Windows — use one of:
#   pip install pipwin && pipwin install pyaudio
#   pip install PyAudio-0.2.11-cp38-cp38-win_amd64.whl  (included in repo)
#
# PyAudio on Linux:
#   sudo apt-get install portaudio19-dev libasound-dev
#   pip install pyaudio
# ─────────────────────────────────────────────────────────────────────────────
